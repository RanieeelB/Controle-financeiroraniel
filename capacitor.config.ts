import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ranieelb.controlefinanceiro',
  appName: 'Saldo Real',
  webDir: 'dist',
};

export default config;
